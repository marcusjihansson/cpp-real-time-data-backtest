# Example .env (do not commit your real keys)
# Copy to .env and fill in values if you plan to use private endpoints

# For ccapi credentials mapping:
BINANCE_API_KEY=your_key
BINANCE_API_SECRET=your_secret
BYBIT_API_KEY=your_key
BYBIT_SECRET_KEY=your_secret
